package models

import "testing"

func TestModels(t *testing.T) {
	// This test is here to ensure the models package is considered in coverage reports.
	// Since the models package only contains struct definitions, there is no code to test.
}
